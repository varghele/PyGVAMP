# docs/requirements.txt
# Minimal requirements for building documentation

# Core documentation
sphinx>=4.0
sphinx-rtd-theme
sphinx-autodoc-typehints
myst-parser
numpydoc

# Core scientific packages (needed for autodoc)
numpy
scipy
matplotlib
pandas
scikit-learn
tqdm

# PyTorch (CPU version for docs)
torch
torchvision
torchaudio

# PyTorch Geometric (base package only)
torch_geometric

# MD analysis
mdtraj

# Other dependencies
joblib
gensim

# Note: The following packages are excluded because they require
# special installation or CUDA support not available on Read the Docs:
# - pyg_lib (requires special wheel installation)
# - torch_scatter (requires compilation)
# - torch_sparse (requires compilation)
# - torch_cluster (requires compilation)
# - torch_spline_conv (requires compilation)
